locals {
  fw_name          = "fw-hub-${var.prefix}-${var.environment}-${var.location}"
  fw_pip_name      = "pip-fw-${var.prefix}-${var.environment}-${var.location}"
  fw_rg_name       = "rg-fw-${var.prefix}-${var.environment}-${var.location}"
  fw_policy_name   = "fw-policy-${var.prefix}-${var.environment}-${var.location}"
  }
